import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color background = Color(0xFF091A4B);
  static const Color primary = Color(0xFF5200FF);
  static const Color secondary = Color(0xFF1B2B61);

  // faded white used for subtitle / secondary text
  static const Color textFaded = Color(0x99FFFFFF); // ~60% white
}
